@{
    ViewData["Title"] = "Home";
    Layout = "_Layout";
}

<div class="home-container">
    <h1 class="title">Welcome to Smart Calculator</h1>
    <p class="subtitle">Perform complex expressions, percentages, and more — all in one place.</p>

    <a asp-controller="Math" asp-action="Index" class="glow-button">Go to Calculator</a>
</div>

<style>
    body {
        background: linear-gradient(135deg, #1e293b, #0f172a);
        font-family: 'Segoe UI', sans-serif;
        color: #f1f5f9;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        text-align: center;
    }

    .home-container {
        max-width: 600px;
        padding: 40px;
    }

    .title {
        font-size: 2.5rem;
        margin-bottom: 20px;
        color: #38bdf8;
        text-shadow: 0 0 10px rgba(56, 189, 248, 0.7);
    }

    .subtitle {
        font-size: 1.1rem;
        color: #cbd5e1;
        margin-bottom: 40px;
    }

    .glow-button {
        background: #2563eb;
        color: white;
        padding: 15px 40px;
        border-radius: 8px;
        text-decoration: none;
        font-size: 1.2rem;
        font-weight: 600;
        box-shadow: 0 0 10px #2563eb, 0 0 20px #1d4ed8;
        transition: all 0.2s ease-in-out;
    }

        .glow-button:hover {
            background: #1d4ed8;
            box-shadow: 0 0 20px #1d4ed8, 0 0 40px #1e40af;
            transform: scale(1.05);
        }
</style>